const express = require('express');
const mongoose = require('mongoose');
const passport = require('passport');
const tripSchema = require('../models/User');
const router = express.Router();



module.exports = router;

